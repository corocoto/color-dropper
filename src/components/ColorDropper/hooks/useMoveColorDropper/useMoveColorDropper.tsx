import {useCallback, useEffect, useRef} from "react";
import {getCursorPosition} from "../../utils";
import {UseMoveColorDropperProps} from "./useMoveColorDropper.types";

const borderWidth = 3;


const useMoveColorDropper = ({canvas, glass, canvasSize, zoom}: UseMoveColorDropperProps) => {


    // return [handleMove];
}

export default useMoveColorDropper;